# React Bits' @react-three/drei@10.x lists react@^19 as a peer, but the
# project is on react@18.3.x. drei works fine on 18 in practice — pin
# legacy-peer-deps so npm install (locally and on Render) doesn't ERESOLVE.
legacy-peer-deps=true
