node_modules/
dist/
.DS_Store
.env
.env.*
!.env.example
# Holds no secret — just the public API URL. Committed so a fresh clone and
# any host that builds from git (Vercel) produce a bundle pointing at the
# real API without depending on a dashboard setting.
!.env.production
*.log
.vite/
.cache/
coverage/
frontend
backend

# Generated by scripts/inject-rpc.mjs at build time
.env.production.local
.vercel

# TypeScript incremental build artifact
tsconfig.tsbuildinfo
