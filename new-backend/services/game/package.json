{
  "name": "@chains/game",
  "version": "2.0.0",
  "private": true,
  "description": "Chains TCG game service — boardgame.io match server + hardened lobby + authoritative match results (:4003)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "dev": "node --watch --enable-source-maps dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@chains/shared": "*",
    "bgio-postgres": "^1.0.16",
    "boardgame.io": "^0.50.2",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.10.5",
    "typescript": "^5.9.2"
  }
}
